project_name: awseventgenerator

builds:
- skip: true

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ .Version }}"

release:
  footer: |
    **Full Changelog**: https://github.com/webdestroya/awseventgenerator/compare/{{ .PreviousTag }}...{{ .Tag }}

changelog:
  skip: true